
<pre>
<font face="Century Gothic" size=3>
           Store: <strong> <%= @bill.store.upcase %> </strong>

  Bought on: <strong> <%= @bill.bought_on.strftime("%d-%b-%Y") %> </strong>

            Total: <strong> <%= number_to_currency(@bill.total) %> </strong>
</font>
</pre>


<%= link_to 'Edit', edit_bill_path(@bill),  class: "btn btn-warning btn-xs active" %>
<%= link_to 'Destroy', bill_path(@bill) , method: :delete, data: {confirm: 'Are you sure you want to destroy the Bill?'}, class: "btn btn-danger active btn-xs" %>
<%= link_to 'Back', bills_path, class: "btn btn-primary btn-xs active"  %>

